# ============================================================
# requirements.txt — migration Django 5.2.12 LTS / Python 3.14
# ============================================================

# --- CORE DJANGO ---
Django==5.2.12                     # LTS jusqu'à avril 2028 (était 4.2.27)
asgiref==3.8.1                     # Django 5.2 requiert >=3.8 (était 3.6.0)
sqlparse==0.5.3                    # bumped (était 0.4.3)

# --- DJANGO PACKAGES ---
django-activity-stream==2.0.0      # à tester avec Django 5.2
django-annoying==0.10.6
django-celery-beat==2.8.0         # bumped pour compat Django 5.x (était 2.6.0)
django-ckeditor==6.7.1             # bumped (était 6.3.2) — surveiller django-ckeditor-5 comme remplacement futur
django-cleanup==8.1.0              # bumped (était 7.0.0)
django-csp==3.8                    # bumped (était 3.7)
django-debug-toolbar==4.4.6        # bumped — 3.x non compatible Django 5.x (était 3.8.1)
django-email-obfuscator==0.1.5
django-environ==0.11.2             # bumped (était 0.9.0)
django-extensions==3.2.3           # bumped (était 3.2.1)
django-filter==24.3                # bumped (était 22.1)
django-js-asset==2.0.0
django-model-utils==4.5.1          # bumped (était 4.3.1)
django-ordered-model==3.7.4        # bumped (était 3.6)
django-permissions-policy==4.21.0  # bumped (était 4.15.0)
django-sendfile2==0.7.1
django-session-timeout==0.1.0
django-settings-export==1.2.1
django-soft-delete==0.9.21         # peu maintenu — à surveiller
django-timezone-field==7.0         # bumped (était 5.0)
djangorestframework==3.16.0        # bumped — support officiel Django 5.2 (était 3.14.0)

# SUPPRIMÉS :
# django-admin==2.0.2       → remplacé par ReadOnlyModelAdmin natif Django 5.2
# django-excel-base==1.0.7  → non utilisé dans le code
# django-excel-response2==3.0.5 → non utilisé dans le code
# django-six==1.0.5         → dépendance de django-admin/excel-response, plus nécessaire
# six==1.16.0               → idem, compat Python 2/3 obsolète

# --- MOZILLA OIDC / KEYCLOAK ---
mozilla-django-oidc==4.0.1         # bumped (était 3.0.0)
python-keycloak==3.12.0            # bumped (était 1.8.1) — breaking changes à vérifier !
josepy==1.14.0                     # bumped (était 1.13.0)

# --- CELERY / ASYNC ---
celery==5.4.0                      # bumped (était 5.2.7)
amqp==5.2.0                        # bumped (était 5.1.1)
billiard==4.2.1                    # bumped (était 3.6.4.0)
kombu==5.3.7                       # bumped (était 5.2.4)
vine==5.1.0                        # bumped (était 5.0.0)
redis==5.0.8                       # bumped (était 4.5.1)
async-timeout==4.0.3               # bumped (était 4.0.2)

# --- CRYPTO / SÉCURITÉ ---
cryptography==46.0.6               # épinglé (était >=43.0.0)
pyOpenSSL==26.0.0                  # épinglé (était >=24.2.1)
cffi==2.0.0                        # épinglé (était >=1.17.0, était 1.15.1)
pycparser==2.21
python-jose==3.5.0
ecdsa==0.19.0                      # bumped (était 0.18.0)
rsa==4.9
pyasn1==0.6.0                      # bumped (était 0.5.1)

# --- HTTP / RÉSEAU ---
requests==2.32.3                   # bumped (était 2.28.2)
urllib3==2.2.1                     # bumped — 1.x EOL (était 1.26.14)
certifi==2024.2.2                  # bumped (était 2022.12.7)
idna==3.7                          # bumped (était 3.4)
charset-normalizer==3.3.2          # bumped (était 3.0.1)

# --- BASE DE DONNÉES ---
psycopg[binary]==3.3.3           # bumped (était 2.9.5)

# --- DOCUMENTS (docx, excel) ---
python-docx==1.1.2                 # bumped — package renommé (était 0.8.11)
docxtpl==0.17.0                    # bumped (était 0.16.5)
docxcompose==2.1.0                 # épinglé (était >=1.5.0, était 1.4.0) — dépendance de docxtpl
lxml==5.2.2                        # bumped (était 4.9.2)
et-xmlfile==1.1.0
XlsxWriter==3.2.0                  # bumped (était 3.0.8)
# xlwt==1.3.0                      # EOL, ne supporte que .xls (Python 2 era) — à supprimer si possible

# --- TEMPLATES / FORMATS ---
Jinja2==3.1.4                      # bumped (était 3.1.2)
MarkupSafe==2.1.5                  # bumped (était 2.1.2)

# --- UTILITAIRES ---
python-dateutil==2.9.0             # bumped (était 2.8.2)
python-dotenv==1.0.1               # bumped (était 0.21.1)
python-magic==0.4.27
pytz==2024.1                       # bumped (était 2022.7.1)
tzdata==2024.1                     # bumped (était 2022.7)
packaging==24.0                    # bumped (était 23.0)
wrapt==1.16.0                      # bumped (était 1.14.1)
Deprecated==1.2.14                 # bumped (était 1.2.13)
typing_extensions==4.11.0          # bumped (était 4.5.0)
pyparsing==3.1.2                   # bumped (était 3.0.9)
python-crontab==3.2.0              # bumped (était 2.7.1)

# --- CLI ---
click==8.1.7                       # bumped (était 8.1.3)
click-didyoumean==0.3.0
click-plugins==1.1.1
click-repl==0.3.0                  # bumped (était 0.2.0)
prompt-toolkit==3.0.47             # bumped (était 3.0.38)
wcwidth==0.2.13                    # bumped (était 0.2.6)

# --- API / SCHEMA ---
# coreapi, coreschema, itypes — SUPPRIMÉS : coreapi est abandonné (2017),
# incompatible Python 3.13, et include_docs_urls est déprécié dans DRF 3.15+.
uritemplate==4.1.1

# --- SERVEUR ---
uWSGI==2.0.31                      # bumped (était 2.0.21)
whitenoise==6.7.0                  # bumped (était 6.4.0)

# --- DIVERS ---
cached-property==1.5.2
importlib-metadata==7.1.0          # bumped (était 4.13.0)
zipp==3.18.1                       # bumped (était 3.15.0)
screen==1.0.1
setuptools==82.0.1                 # épinglé (était >=70.0)